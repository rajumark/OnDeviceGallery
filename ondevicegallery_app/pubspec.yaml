name: ondevicegallery_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Key features required for the project
  provider: ^6.0.5
  permission_handler: ^11.3.0
  sqflite: ^2.3.3
  path_provider: ^2.1.5
  google_mlkit_text_recognition: ^0.15.1
  share_plus: ^9.0.0
  image_picker: ^1.2.2

flutter:
  uses-material-design: true
